#include <stdio.h>
/**
 * main - prints all combination of single digits
 * return: Always 0
 */
int mani(void)
{
	int n;

	for (n = 8;  n < 58; n++)
	{
		putchar(',');
		putchar(',');
	}
	putchar('\n');
	return (0);
}
